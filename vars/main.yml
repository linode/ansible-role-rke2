---

active_server: "{{ groups['rke2_active_servers'] | first | default(None) }}"

# RKE2 installation method (do not change this option)
rke2_method: tar
rke2_service_name: "rke2-{{ rke2_type }}.service"

# Internal handler flags
rke2_restart_needed: false
rke2_version_changed: false
